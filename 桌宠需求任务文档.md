# 桌宠需求任务文档（先 MVP，后迭代）

> 目的：把“桌宠完整技术方案”拆成一份可以按顺序实施的任务文档。先用最小成本做出能跑、可爱、好玩的 MVP；后续按模块逐步加能力，避免一开始就把项目做复杂。

## 1. 交付原则

1. 先做“离线可玩”：不依赖大模型、不依赖网络，也能好玩。
2. 先做“稳定常驻”：窗口、托盘、拖拽、缩放、锁定穿透必须可靠。
3. 先做“扩展口子”，再做“扩展内容”：先把台词包/宠物包导入机制跑通，后续再堆数量与玩法。
4. 可选能力都默认关闭：AI、TTS、记忆、主动互动等，做成开关模块。

## 2. MVP 定义（必须达到）

MVP 的定义是：**桌宠能常驻桌面 + 用户点一下就会有可爱的反馈 + 支持导入台词包/宠物包 + 设置里能控制打扰程度**。

### MVP 功能范围

- 桌面悬浮窗口：透明、无边框、置顶、拖拽移动、缩放
- 点击穿透与锁定：锁定后不误触
- 托盘菜单：显示/隐藏、锁定/解锁、打开设置、退出
- 角色渲染：先支持一种（建议 spritesheet/WebP/GIF 之一即可）
- 交互事件：`tap`、`pet(长按)`、`drag`、`scale`、`enter/exit`、`idle`
- 台词引擎：按事件选台词（权重/冷却/过滤），在气泡里显示短句
- 本地数据：配置持久化（userData），支持一键恢复默认
- 资源导入：导入台词包（zip），启用/禁用，导入时做安全校验

## 3. 里程碑与任务列表

说明：
- “验收标准”写的是完成与否的判断方式。
- 任务按依赖顺序排列；同一里程碑内部可以并行做，但建议按序推进减少返工。

### M0 立项与骨架（最先做）

| ID | 任务 | 产出 | 验收标准 |
|---|---|---|---|
| M0-1 | 用 `electron-vite` 初始化项目（建议 `react-ts`） | 可运行的空壳应用 | `npm run dev` 能启动 Electron；`npm run build` 能生成 `out/`；`package.json.main` 指向 `./out/main/index.js` |
| M0-2 | 建立目录分层（main/preload/renderer/shared） | 工程结构落地 | `src/main`、`src/preload`、`src/renderer`、`src/shared` 均存在且职责清晰；跨进程类型放在 `shared` |
| M0-3 | 建立 IPC 约定（通道白名单 + invoke/handle） | IPC 基建 | 渲染进程不直接引用 `ipcRenderer`；只通过 `window.api.*` 调用主进程 |
| M0-4 | 本地数据目录与配置文件（userData/config.json） | ConfigStore | 改动设置能落盘；重启后设置能恢复；提供“恢复默认”按钮 |
| M0-5 | 日志与错误兜底（基础即可） | 日志文件/错误提示 | 主进程异常不会直接白屏；日志能定位导入失败、配置解析失败等关键问题 |

### M1 MVP：窗口、托盘、交互与气泡

| ID | 任务 | 产出 | 验收标准 |
|---|---|---|---|
| M1-1 | 透明无边框窗口 + 置顶 + 初始位置策略 | 主窗口可用 | 桌宠启动后出现在屏幕边缘（如右下角）；重启后位置可恢复（可选） |
| M1-2 | 拖拽移动（临时关闭穿透或按住拖拽区） | 拖拽交互 | 用户能把桌宠拖到任意位置；拖拽过程不卡顿 |
| M1-3 | 缩放（滚轮或设置滑条） | 缩放能力 | 缩放范围可配置（如 0.6~1.8）；缩放后不糊/不闪（以你当前渲染方案为准） |
| M1-4 | 点击穿透模式（跟随鼠标/完全穿透）+ 锁定位置 | 防误触 | 锁定后桌宠不影响日常点击；解锁后可继续拖拽/交互 |
| M1-5 | 托盘菜单（显示/隐藏、锁定/解锁、设置、退出） | 托盘功能 | 托盘图标存在且可用；菜单操作即时生效；退出能彻底关闭进程 |
| M1-6 | 气泡 UI（短句显示） | 气泡组件 | 触发互动时能在桌宠旁显示短句；文字不遮挡过多；气泡可开关 |

### M2 MVP：渲染（先 Sprite）与交互事件标准化

| ID | 任务 | 产出 | 验收标准 |
|---|---|---|---|
| M2-1 | Sprite 渲染最小实现（选一种：spritesheet/WebP/GIF） | PetRuntime（最小） | 桌宠能显示动画或图片；渲染帧率稳定（可先 30fps） |
| M2-2 | 交互事件标准化（tap/pet/drag/scale/idle） | Interaction 模块 | 每类事件都能稳定触发；事件有统一结构（type + 参数） |
| M2-3 | 部位命中（可选，先做粗粒度也行） | bodyPart 产出 | 至少能区分 `head/body`（或 `face/body`）；用于台词选择 |
| M2-4 | 音效播放（可选） | SFX 播放器 | 支持播放短音效；音量可在设置里控制；默认可关闭 |

### M3 MVP：台词引擎与台词包导入（离线可玩核心）

| ID | 任务 | 产出 | 验收标准 |
|---|---|---|---|
| M3-1 | QuoteEngine：按事件选台词（权重/冷却/过滤） | 台词引擎 | 同一事件有多条台词轮换；不会刷屏；支持工作模式（静音/仅触发） |
| M3-2 | 台词包格式落地（manifest + quotes.jsonl） | Pack Schema | 能解析示例包；解析失败时有明确错误提示 |
| M3-3 | 导入台词包（zip → 校验 → 落盘 → 启用） | PackImporter（quote） | 能导入你现有示例台词包；导入后立即生效；重启仍生效 |
| M3-4 | 导入校验与安全策略（白名单/路径逃逸/大小限制） | 安全校验 | 不合法 zip 会被拒绝；拒绝原因清晰；不会写出 userData 目录外 |
| M3-5 | 台词包管理 UI（启用/禁用/删除/重载） | 设置页子模块 | 可以切换台词包；禁用后不再触发；删除后本地文件被清理 |

到这里，MVP 已完成：离线可玩、常驻、好玩、可扩展。

## 4. 后续迭代路线（按需选做）

### I1 宠物包体系（角色扩展）

| ID | 任务 | 产出 | 验收标准 |
|---|---|---|---|
| I1-1 | 宠物包格式（pet.json + assets） | PetPack Schema | 能描述 petId/name/type/资源路径/默认动作等 |
| I1-2 | 导入宠物包（zip） | PackImporter（pet） | 导入后在“角色列表”出现；可切换；重启仍存在 |
| I1-3 | 角色列表 UI（内置 + 导入） | 角色选择页 | 切换不卡死；切换后资源释放（至少不持续涨内存） |
| I1-4 | 渲染降级路线 | fallback 逻辑 | Live2D 不可用时自动用 sprite；资源损坏时回退到默认角色 |

### I2 Live2D（可选高级但常见）

| ID | 任务 | 产出 | 验收标准 |
|---|---|---|---|
| I2-1 | Live2D 渲染适配（Pixi Live2D） | Live2DRuntime | 能加载一个可用的 model3.json；基础动作可播放 |
| I2-2 | 表情/动作映射（mood → expression/motion） | 映射配置 | 触发不同 mood 时能切表情/动作；找不到资源时不报错 |
| I2-3 | 授权提示与资源策略 | UI 提示/打包策略 | 发布包默认不内置未授权模型；导入时提示版权风险 |

### I3 轻对话（可选 AI）

| ID | 任务 | 产出 | 验收标准 |
|---|---|---|---|
| I3-1 | LLMService（OpenAI-compatible） | LLM 客户端 | 支持 baseURL/apiKey/model；失败可重试；支持取消 |
| I3-2 | 对话 UI（可复用气泡） | 聊天模式 | 用户输入一句，桌宠回 1-2 句；可关闭 AI |
| I3-3 | “短句幽默”提示词模板 | Prompt 模板 | 输出风格稳定（短、口语、幽默、不办公） |
| I3-4 | 情绪尾标（可选） | mood 输出 | 模型回复可带 `[mood: happy]` 之类结构，驱动动作/表情 |

### I4 TTS（可选语音）

| ID | 任务 | 产出 | 验收标准 |
|---|---|---|---|
| I4-1 | TTSService（先做 Edge TTS 或本地 HTTP） | 语音播放 | 开关可控；音量可调；不影响离线玩法 |
| I4-2 | 口型同步（简化版） | lip-sync | 播放音频时嘴型/抖动有变化（简单即可） |
| I4-3 | 多引擎适配（再做） | provider 扩展 | 可按 provider 切换；配置缺失时给出提示 |

### I5 轻量记忆（可选）

| ID | 任务 | 产出 | 验收标准 |
|---|---|---|---|
| I5-1 | 本地记忆文件（facts） | memory.json | 能保存昵称/偏好；一键清空 |
| I5-2 | 摘要压缩（可选） | summary | 达到阈值才触发；触发频率可控；用户可关闭 |

### I6 皮肤/主题（提升“可爱”氛围）

| ID | 任务 | 产出 | 验收标准 |
|---|---|---|---|
| I6-1 | 主题系统（颜色/字体/气泡样式） | Theme 模块 | 切换主题立即生效；重启保留 |
| I6-2 | 皮肤包导入（zip） | PackImporter（skin） | 可导入贴纸/音效/气泡样式；不影响核心逻辑 |

## 5. 建议的实施顺序（最稳）

1. M0 → M1：先把工程骨架、窗口、托盘、设置跑稳。
2. M2：把最小渲染与交互事件跑通（不用追求完美动画）。
3. M3：把台词引擎 + 台词包导入做成“离线也好玩”的核心。
4. I1/I2：角色扩展与 Live2D（看你是否要押注 Live2D 生态）。
5. I3/I4/I5：AI/TTS/记忆按需加，不要影响离线玩法。

## 6. 验收清单（MVP）

| 项 | 通过标准 |
|---|---|
| 启动 | `npm run dev` 能启动；安装包（可后置）启动不白屏 |
| 常驻 | 透明无边框、置顶、托盘、隐藏/显示可用 |
| 交互 | tap/pet/drag/scale 能触发反馈；锁定/穿透不误触 |
| 好玩 | 离线台词库足够（至少 50 条），不会频繁重复、不会刷屏 |
| 扩展 | 能导入台词包（zip），启用/禁用/删除生效且重启保持 |
| 安全 | 导入校验有效：拒绝危险文件/路径逃逸 |

