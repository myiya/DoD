# 桌宠需求任务文档（先 MVP，后迭代）

> 目的：把“桌宠完整技术方案”拆成一份可以按顺序实施的任务文档。先用最小成本做出能跑、可爱、好玩的 MVP；后续按模块逐步加能力，避免一开始就把项目做复杂。

## 1. 交付原则

1. 先做“离线可玩”：不依赖大模型、不依赖网络，也能好玩。
2. 先做“稳定常驻”：窗口、托盘、拖拽、缩放、锁定穿透必须可靠。
3. 先做“扩展口子”，再做“扩展内容”：先把台词包/宠物包导入机制跑通，后续再堆数量与玩法。
4. 可选能力都默认关闭：AI、TTS、记忆、主动互动等，做成开关模块。

## 2. MVP 定义（必须达到）

MVP 的定义是：**桌宠能常驻桌面 + 用户点一下就会有可爱的反馈 + 支持导入台词包/宠物包 + 设置里能控制打扰程度**。

### MVP 功能范围

- 桌面悬浮窗口：透明、无边框、置顶、拖拽移动、缩放
- 点击穿透与锁定：锁定后不误触
- 托盘菜单：显示/隐藏、锁定/解锁、打开设置、退出
- 角色渲染：先支持一种（建议 spritesheet/WebP/GIF 之一即可）
- 交互事件：`tap`、`pet(长按)`、`drag`、`scale`、`enter/exit`、`idle`
- 台词引擎：按事件选台词（权重/冷却/过滤），在气泡里显示短句
- 本地数据：配置持久化（userData），支持一键恢复默认
- 资源导入：导入台词包（zip），启用/禁用，导入时做安全校验

## 3. 里程碑与任务列表

说明：
- “验收标准”写的是完成与否的判断方式。
- 任务按依赖顺序排列；同一里程碑内部可以并行做，但建议按序推进减少返工。

### M0 立项与骨架（最先做）

| ID | 状态 | 任务 | 产出 | 验收标准 |
|---|---|---|---|---|
| M0-1 | 已完成 | 用 `electron-vite` 初始化项目（建议 `react-ts`） | 可运行的空壳应用 | `npm run dev` 能启动 Electron；`npm run build` 能生成 `out/`；`package.json.main` 指向 `./out/main/index.js` |
| M0-2 | 已完成 | 建立目录分层（main/preload/renderer/shared） | 工程结构落地 | `src/main`、`src/preload`、`src/renderer`、`src/shared` 均存在且职责清晰；跨进程类型放在 `shared` |
| M0-3 | 已完成 | 建立 IPC 约定（通道白名单 + invoke/handle） | IPC 基建 | 渲染进程不直接引用 `ipcRenderer`；只通过 `window.api.*` 调用主进程 |
| M0-4 | 已完成 | 本地数据目录与配置文件（userData/config.json） | ConfigStore | 改动设置能落盘；重启后设置能恢复；提供“恢复默认”按钮 |
| M0-5 | 已完成 | 日志与错误兜底（基础即可） | 日志文件/错误提示 | 主进程异常不会直接白屏；日志能定位导入失败、配置解析失败等关键问题 |

### M1 MVP：窗口、托盘、交互与气泡

| ID | 状态 | 任务 | 产出 | 验收标准 |
|---|---|---|---|---|
| M1-1 | 已完成 | 透明无边框窗口 + 置顶 + 初始位置策略 | 主窗口可用 | 桌宠启动后出现在屏幕边缘（如右下角）；重启后位置可恢复（可选） |
| M1-2 | 已完成 | 拖拽移动（临时关闭穿透或按住拖拽区） | 拖拽交互 | 用户能把桌宠拖到任意位置；拖拽过程不卡顿 |
| M1-3 | 已完成 | 缩放（滚轮或设置滑条） | 缩放能力 | 缩放范围可配置（如 0.6~1.8）；缩放后不糊/不闪（以你当前渲染方案为准） |
| M1-4 | 已完成（简化） | 点击穿透模式（跟随鼠标/完全穿透）+ 锁定位置 | 防误触 | 锁定后桌宠不影响日常点击；解锁后可继续拖拽/交互 |
| M1-5 | 已完成 | 托盘菜单（显示/隐藏、锁定/解锁、设置、退出） | 托盘功能 | 托盘图标存在且可用；菜单操作即时生效；退出能彻底关闭进程 |
| M1-6 | 已完成 | 气泡 UI（短句显示） | 气泡组件 | 触发互动时能在桌宠旁显示短句；文字不遮挡过多；气泡可开关 |

### M2 MVP：渲染（先 Sprite）与交互事件标准化

| ID | 状态 | 任务 | 产出 | 验收标准 |
|---|---|---|---|---|
| M2-1 | 已完成 | Sprite 渲染最小实现（选一种：spritesheet/WebP/GIF） | PetRuntime（最小） | 桌宠能显示动画或图片；渲染帧率稳定（可先 30fps） |
| M2-2 | 已完成 | 交互事件标准化（tap/pet/drag/scale/idle） | Interaction 模块 | 每类事件都能稳定触发；事件有统一结构（type + 参数） |
| M2-3 | 已完成 | 部位命中（可选，先做粗粒度也行） | bodyPart 产出 | 至少能区分 `head/body`（或 `face/body`）；用于台词选择 |
| M2-4 | 已完成（可选） | 音效播放（可选） | SFX 播放器 | 支持播放短音效；音量可在设置里控制；默认可关闭 |

### M3 MVP：台词引擎与台词包导入（离线可玩核心）

| ID | 状态 | 任务 | 产出 | 验收标准 |
|---|---|---|---|---|
| M3-1 | 已完成 | QuoteEngine：按事件选台词（权重/冷却/过滤） | 台词引擎 | 同一事件有多条台词轮换；不会刷屏；支持工作模式（静音/仅触发） |
| M3-2 | 已完成 | 台词包格式落地（manifest + quotes.jsonl） | Pack Schema | 能解析示例包；解析失败时有明确错误提示 |
| M3-3 | 已完成 | 导入台词包（zip → 校验 → 落盘 → 启用） | PackImporter（quote） | 能导入你现有示例台词包；导入后立即生效；重启仍生效 |
| M3-4 | 已完成 | 导入校验与安全策略（白名单/路径逃逸/大小限制） | 安全校验 | 不合法 zip 会被拒绝；拒绝原因清晰；不会写出 userData 目录外 |
| M3-5 | 已完成 | 台词包管理 UI（启用/禁用/删除/重载） | 设置页子模块 | 可以切换台词包；禁用后回退内置默认包；删除后本地文件被清理 |

到这里，MVP 已完成：离线可玩、常驻、好玩、可扩展。

### M4：设置页重构与日志配置（真实设置）

目标：把“设置页”从开发期的验证面板，升级为桌宠真实可用的设置中心；移除所有预览/验证功能；补齐日志级别/清空/打开目录等操作。

| ID | 状态 | 任务 | 产出 | 验收标准 |
|---|---|---|---|---|
| M4-1 | 已完成 | 设置页信息架构重做（左侧分类导航 + 右侧内容区） | 新 Settings UI | 左侧分类：`桌宠窗口/互动/音效/台词包/日志/关于`；右侧随分类切换；不再出现 `PetStage` 预览/事件日志/debug spritesheet 等验证内容 |
| M4-2 | 已完成 | 日志配置项（区分级别） | LogConfig（config + normalize） | 默认有日志；支持设置日志级别（`INFO/WARN/ERROR`）并持久化到 `config.json` |
| M4-3 | 已完成 | LoggerService 支持日志级别过滤 | LoggerService | 设置为 `WARN` 时不写入 INFO；设置为 `ERROR` 时仅写 ERROR；不影响开发环境控制台输出（仅控制落盘） |
| M4-4 | 已完成 | 日志操作：清空日志 / 打开日志目录（写入项目日志文件存储） | LogOps + IPC | 设置页按钮可清空 `userData/logs/app.log`；可一键打开 `userData/logs/` 目录；失败时有明确提示 |
| M4-5 | 已完成 | 设置页接入日志配置与操作 | UI 接入 | 设置页中“日志”分类可设置级别、清空日志、打开目录；与主进程 IPC 通信稳定 |
| M4-6 | 已完成 | 文档与验收 | 文档更新 | 更新本任务文档的状态与实施记录；`npm run typecheck`/`npm run lint` 通过 |

## 4. 后续迭代路线（按需选做）

### I1 宠物包体系（角色扩展）

| ID | 状态 | 任务 | 产出 | 验收标准 |
|---|---|---|---|---|
| I1-1 | 未开始 | 宠物包格式（pet.json + assets） | PetPack Schema | 能描述 petId/name/type/资源路径/默认动作等 |
| I1-2 | 未开始 | 导入宠物包（zip） | PackImporter（pet） | 导入后在“角色列表”出现；可切换；重启仍存在 |
| I1-3 | 未开始 | 角色列表 UI（内置 + 导入） | 角色选择页 | 切换不卡死；切换后资源释放（至少不持续涨内存） |
| I1-4 | 未开始 | 渲染降级路线 | fallback 逻辑 | Live2D 不可用时自动用 sprite；资源损坏时回退到默认角色 |

### I2 Live2D（可选高级但常见）

| ID | 状态 | 任务 | 产出 | 验收标准 |
|---|---|---|---|---|
| I2-1 | 未开始 | Live2D 渲染适配（Pixi Live2D） | Live2DRuntime | 能加载一个可用的 model3.json；基础动作可播放 |
| I2-2 | 未开始 | 表情/动作映射（mood → expression/motion） | 映射配置 | 触发不同 mood 时能切表情/动作；找不到资源时不报错 |
| I2-3 | 未开始 | 授权提示与资源策略 | UI 提示/打包策略 | 发布包默认不内置未授权模型；导入时提示版权风险 |

### I3 轻对话（可选 AI）

| ID | 状态 | 任务 | 产出 | 验收标准 |
|---|---|---|---|---|
| I3-1 | 未开始 | LLMService（OpenAI-compatible） | LLM 客户端 | 支持 baseURL/apiKey/model；失败可重试；支持取消 |
| I3-2 | 未开始 | 对话 UI（可复用气泡） | 聊天模式 | 用户输入一句，桌宠回 1-2 句；可关闭 AI |
| I3-3 | 未开始 | “短句幽默”提示词模板 | Prompt 模板 | 输出风格稳定（短、口语、幽默、不办公） |
| I3-4 | 未开始 | 情绪尾标（可选） | mood 输出 | 模型回复可带 `[mood: happy]` 之类结构，驱动动作/表情 |

### I4 TTS（可选语音）

| ID | 状态 | 任务 | 产出 | 验收标准 |
|---|---|---|---|---|
| I4-1 | 未开始 | TTSService（先做 Edge TTS 或本地 HTTP） | 语音播放 | 开关可控；音量可调；不影响离线玩法 |
| I4-2 | 未开始 | 口型同步（简化版） | lip-sync | 播放音频时嘴型/抖动有变化（简单即可） |
| I4-3 | 未开始 | 多引擎适配（再做） | provider 扩展 | 可按 provider 切换；配置缺失时给出提示 |

### I5 轻量记忆（可选）

| ID | 状态 | 任务 | 产出 | 验收标准 |
|---|---|---|---|---|
| I5-1 | 未开始 | 本地记忆文件（facts） | memory.json | 能保存昵称/偏好；一键清空 |
| I5-2 | 未开始 | 摘要压缩（可选） | summary | 达到阈值才触发；触发频率可控；用户可关闭 |

### I6 皮肤/主题（提升“可爱”氛围）

| ID | 状态 | 任务 | 产出 | 验收标准 |
|---|---|---|---|---|
| I6-1 | 未开始 | 主题系统（颜色/字体/气泡样式） | Theme 模块 | 切换主题立即生效；重启保留 |
| I6-2 | 未开始 | 皮肤包导入（zip） | PackImporter（skin） | 可导入贴纸/音效/气泡样式；不影响核心逻辑 |

## 5. 建议的实施顺序（最稳）

1. M0 → M1：先把工程骨架、窗口、托盘、设置跑稳。
2. M2：把最小渲染与交互事件跑通（不用追求完美动画）。
3. M3：把台词引擎 + 台词包导入做成“离线也好玩”的核心。
4. I1/I2：角色扩展与 Live2D（看你是否要押注 Live2D 生态）。
5. I3/I4/I5：AI/TTS/记忆按需加，不要影响离线玩法。

## 6. 验收清单（MVP）

| 项 | 通过标准 |
|---|---|
| 启动 | `npm run dev` 能启动；安装包（可后置）启动不白屏 |
| 常驻 | 透明无边框、置顶、托盘、隐藏/显示可用 |
| 交互 | tap/pet/drag/scale 能触发反馈；锁定/穿透不误触 |
| 好玩 | 离线台词库足够（至少 50 条），不会频繁重复、不会刷屏 |
| 扩展 | 能导入台词包（zip），启用/禁用/删除生效且重启保持 |
| 安全 | 导入校验有效：拒绝危险文件/路径逃逸 |

## 7. 实施记录

### 2026-09-09：M0 已实现

当前已在 `d:\workspace\DoD\electron-dod` 完成 M0 立项与骨架阶段，已落地内容如下：

#### 已完成

- `M0-1` 用 `electron-vite` 初始化项目
  - 当前项目已基于 `electron-vite + React + TypeScript` 建立并可运行。
- `M0-2` 建立目录分层
  - 已补充 `src/shared`
  - 已将主进程拆分出 `bootstrap / constants / ipc / services`
- `M0-3` 建立 IPC 约定
  - 渲染进程不再直接依赖 `ipcRenderer`
  - 已通过 `window.api.app.*` 与 `window.api.config.*` 暴露白名单接口
- `M0-4` 本地数据目录与配置文件
  - 已实现 `userData/config.json`
  - 支持读取、局部更新、恢复默认
  - 配置损坏时会回退默认值
- `M0-5` 日志与错误兜底
  - 已实现基础日志写入
  - 可记录应用启动、配置初始化、配置读取失败、配置更新失败、IPC 异常

#### 本次落地的主要模块

- `src/shared/types/config.ts`
- `src/shared/types/api.ts`
- `src/shared/constants/config.ts`
- `src/main/bootstrap/create-main-window.ts`
- `src/main/constants/app.ts`
- `src/main/services/config-service.ts`
- `src/main/services/logger-service.ts`
- `src/main/ipc/config.ts`
- `src/main/ipc/register-ipc.ts`
- `src/preload/index.ts`
- `src/preload/index.d.ts`
- `src/renderer/src/pages/settings/SettingsPage.tsx`

#### 当前效果

- 首页已从脚手架模板页替换为“最小设置页”
- 已可读取并修改以下配置：
  - `theme`
  - `petScale`
  - `bubbleEnabled`
  - `interactionMode`
- 已支持“恢复默认配置”

#### 已完成验证

- `npm run typecheck` 通过
- 定向 `eslint` 检查通过

#### 说明

- 当前只完成 M0，不包含托盘、透明桌宠窗口、拖拽/缩放桌宠交互、台词引擎、资源包导入等 MVP 功能
- 下一阶段按原计划进入 `M1 MVP：窗口、托盘、交互与气泡`

#### 当前任务状态汇总

| 阶段 | 状态 |
|---|---|
| M0 | 已完成 |
| M1 | 已完成（简化穿透） |
| M2 | 已完成（含 M2-4） |
| M3 | 已完成 |
| M4 | 已完成 |
| I1 | 未开始 |
| I2 | 未开始 |
| I3 | 未开始 |
| I4 | 未开始 |
| I5 | 未开始 |
| I6 | 未开始 |

### 2026-09-09：M2 已实现（基础版）

当前已在 `d:\workspace\DoD\electron-dod` 完成 M2 的“Sprite 渲染 + 交互事件标准化 + bodyPart 命中（粗粒度）”。

#### 已完成（对应任务）

- `M2-1` Sprite 渲染最小实现
  - 当前实现为 Canvas Sprite：默认加载内置 `electron.svg` 并以固定帧率进行“呼吸/漂浮”动画
  - 已补充 `SpriteAsset` 抽象：同一套渲染器支持 `image`（单图）与 `spritesheet`（帧动画）
- `M2-2` 交互事件标准化
  - 已支持 `tap / pet(长按) / dragStart / drag / dragEnd / scale(滚轮) / enter / exit / idle`
  - 拖拽 delta 基于 `screenX/screenY` 全局坐标，避免窗口移动导致 “drag 抖动/拖不动”
  - 事件结构统一，便于后续接台词引擎（M3）
- `M2-3` 部位命中（粗粒度）
  - 当前按纵向区域粗分：`face / head / body`（满足最小验收）

#### 主要落地模块

- `electron-dod/src/shared/types/interaction.ts`
- `electron-dod/src/renderer/src/pet-runtime/`
  - `PetStage.tsx`
  - `canvas/createSpriteRenderer.ts`
  - `types.ts`（SpriteAsset：image / spritesheet）
- `electron-dod/src/renderer/src/pages/settings/SettingsPage.tsx`

#### 当前验证方式

- 在桌宠窗口（PetWindow）进行点按/长按/拖动/滚轮缩放操作，通过“气泡反馈 / 音效反馈 / 位置变化”验证事件链路与渲染稳定性
- 说明：原先设置页的“桌宠预览/事件日志/示例 spritesheet（debug）”验证面板已在 M4 中移除，避免与真实设置混杂

#### M2-4 音效播放（可选）补充

- 已新增轻量音效模块（WebAudio），默认关闭
- 设置页新增：
  - 音效开关 `sfxEnabled`
  - 音效音量 `sfxVolume`（0~100%）
- 当前音效触发策略：
  - `tap` / `pet` 必触发
  - `scale` 有节流（避免滚轮刷屏）
  - 已同步到桌宠窗口（PetWindow）：开启音效后，直接“戳桌宠/摸摸/缩放”即可听到提示音（不需要额外资源文件）

### 2026-09-09：M1 已实现（窗口/托盘/交互/气泡）

当前已在 `d:\workspace\DoD\electron-dod` 完成 M1 阶段能力：桌宠透明置顶窗口、托盘菜单、窗口拖拽移动、缩放、锁定穿透、气泡短句显示。

#### 已完成（对应任务）

- `M1-1` 透明无边框窗口 + 置顶 + 初始位置策略
  - 新增独立桌宠窗口（PetWindow），默认置顶、无边框、透明、常驻
  - 位置会写入 `userData/pet-window-state.json`，重启可恢复
- `M1-2` 拖拽移动
  - 桌宠窗口内拖动触发 `drag` 事件，并通过 IPC 调主进程移动窗口（`moveBy`）
- `M1-3` 缩放
  - 设置页滑条可缩放（已存在）
  - 桌宠窗口支持滚轮缩放（会写回 `config.petScale`）
- `M1-4` 锁定穿透（简化）
  - 当前实现为“锁定（穿透）/解锁（可交互）”二态，锁定后桌宠不接收鼠标事件并透传给下层窗口
  - （未实现“跟随鼠标穿透”这种更复杂模式，后续可选加）
- `M1-5` 托盘菜单
  - 支持：显示/隐藏、锁定/解锁、打开设置、退出
- `M1-6` 气泡 UI
  - 在桌宠窗口内根据事件（tap/pet/drag/scale/idle 等）显示短句气泡
  - 受 `bubbleEnabled` 与 `interactionMode`（quiet）控制

#### 主要落地模块

- 主进程：
  - `electron-dod/src/main/bootstrap/create-pet-window.ts`
  - `electron-dod/src/main/bootstrap/create-tray.ts`
  - `electron-dod/src/main/bootstrap/load-renderer.ts`
  - `electron-dod/src/main/ipc/pet-window.ts`
  - `electron-dod/src/main/services/pet-window-state-service.ts`
- 预加载与共享类型：
  - `electron-dod/src/shared/types/api.ts`
  - `electron-dod/src/preload/index.ts`
- 渲染进程：
  - `electron-dod/src/renderer/src/pages/pet/PetWindowPage.tsx`
  - `electron-dod/src/renderer/src/App.tsx`
  - `electron-dod/src/renderer/src/assets/main.css`

#### 验证方式

- `npm run dev` 启动后应直接出现桌宠窗口（右下角附近）
- 托盘图标：
  - 左键：快速显示/隐藏
  - 右键菜单：显示/隐藏、锁定/解锁、打开设置、退出
- 桌宠窗口：
  - 拖动：移动位置
  - 滚轮：缩放
  - 点击/长按：触发气泡短句

#### 拖拽优化补充

- 修复了“拖动闪烁/拖不动/拖不远”的问题：拖拽 delta 从 `clientX/clientY` 改为 `screenX/screenY`（全局坐标），避免窗口移动后 client 坐标不变导致 delta 归零
- 拖拽气泡增加了节流（避免拖动过程中频繁刷新导致视觉闪烁）
- 增加“窗口不出屏”限制：桌宠窗口移动时会按屏幕工作区（workArea）进行边界裁剪（clamp），并在分辨率/显示器变化时自动拉回屏幕内；默认仍以右下角为初始位置

### 2026-09-09：M3 已实现（台词引擎 + 台词包导入）

当前已完成 M3 的“离线可玩核心”：按交互事件选台词，并支持导入/管理台词包（zip）。

#### 已完成（对应任务）

- `M3-1` QuoteEngine（权重/冷却/过滤）
  - 新增 QuoteEngine：支持全局节流（防刷屏）、单句冷却、权重随机、bodyPart 过滤
  - 默认对 `drag` 不出词，避免拖拽高频刷屏
- `M3-2` 台词包格式
  - `manifest.json` + `quotes.jsonl`（JSONL 一行一条）
  - 内置默认包（builtin）保底：禁用用户包后回退 builtin，仍可离线玩
- `M3-3/M3-4` 导入台词包（zip → 校验 → 落盘）
  - 主进程负责导入与安全校验：大小限制、必须文件校验、路径逃逸拦截、行数/长度限制
  - `packId` 冲突时弹窗询问“覆盖/取消”
- `M3-5` 台词包管理 UI（设置页 + 拖拽导入）
  - 设置页新增“台词包（M3）”区块：按钮导入 + 拖拽导入 + 极简列表（单选启用/禁用回退/删除/重载）

#### 主要落地模块

- 共享类型与内置内容：
  - `electron-dod/src/shared/types/quote.ts`
  - `electron-dod/src/shared/types/quote-pack.ts`
  - `electron-dod/src/shared/constants/builtin-quotes.ts`
- 主进程：
  - `electron-dod/src/main/services/quote-pack-service.ts`
  - `electron-dod/src/main/ipc/quote-pack.ts`
- 预加载：
  - `electron-dod/src/preload/index.ts`（`window.api.quotePack.*`）
- 渲染进程：
  - `electron-dod/src/renderer/src/quotes/quoteEngine.ts`
  - `electron-dod/src/renderer/src/pages/settings/SettingsPage.tsx`（台词包 UI）
  - `electron-dod/src/renderer/src/pages/pet/PetWindowPage.tsx`（QuoteEngine 驱动气泡）

#### 验证方式

- 打开设置页：
  - 在“台词包（M3）”区块导入 `.zip` 或拖拽 `.zip` 到虚线区域
  - 导入成功后，点击“设为启用”
- 回到桌宠窗口：
  - 点击/长按/缩放会触发不同台词（不刷屏，且会轮换）
  - 点击“禁用（回退内置）”后，仍会说内置默认台词

### 2026-09-09：M4 已实现（设置页重构与日志配置）

本次完成了“真实设置页”的重构：删除所有开发期预览/验证功能，改为左侧导航 + 右侧内容区，并补齐日志级别控制、清空日志、一键打开日志目录。

#### 已完成（对应任务）

- `M4-1` 设置页信息架构重做（B：左侧分类导航 + 右侧内容区）
  - 分类：`桌宠窗口/互动/音效/台词包/日志/关于`
  - 已移除：`PetStage` 预览、事件日志、debug spritesheet 等验证功能
- `M4-2` 日志配置项（区分级别）
  - 新增 `config.logLevel`（默认 `INFO`），并完成 normalize/patch
- `M4-3` LoggerService 支持日志级别过滤
  - 仅控制落盘（`userData/logs/app.log`）写入过滤；开发环境控制台输出不受影响
- `M4-4/M4-5` 日志操作与设置页接入
  - 设置页“日志”分类支持：`INFO/WARN/ERROR` 级别选择、`清空日志`、`打开日志目录`
- `M4-6` 文档与验收
  - `npm run typecheck` / `npm run lint` 通过

#### 主要落地模块

- 配置类型与默认值：
  - `electron-dod/src/shared/types/config.ts`（新增 `logLevel`）
  - `electron-dod/src/shared/constants/config.ts`（新增 `LOG_LEVEL_OPTIONS` 与 normalize/patch）
- 主进程日志与配置联动：
  - `electron-dod/src/main/services/logger-service.ts`（支持级别过滤、清空日志、打开目录）
  - `electron-dod/src/main/services/config-service.ts`（读取/更新后同步 logger level）
- IPC / Preload：
  - `electron-dod/src/main/ipc/log.ts`
  - `electron-dod/src/main/ipc/register-ipc.ts`
  - `electron-dod/src/shared/types/api.ts`（新增 `log` API）
  - `electron-dod/src/preload/index.ts`（`window.api.log.*`）
- 设置页与样式：
  - `electron-dod/src/renderer/src/pages/settings/SettingsPage.tsx`（重写：侧边栏导航 + 真实设置）
  - `electron-dod/src/renderer/src/assets/main.css`（新增侧边栏布局样式，移除旧预览相关样式）
